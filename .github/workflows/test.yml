name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:  # Manuel tetikleme için

permissions:
  contents: read

jobs:
  test:
    name: Unit & Integration Tests
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout kodu
        uses: actions/checkout@v4

      - name: 🟢 Node.js kur
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: 📦 Dependencies yükle
        run: npm ci

      - name: 🔍 TypeScript type check
        run: npm run type-check

      - name: 🧹 ESLint check
        run: npm run lint
        continue-on-error: true  # Lint errors shouldn't fail the build initially

      - name: 🧪 Unit tests çalıştır
        run: npm run test:run

      - name: 📊 Code coverage oluştur
        run: npm run test:coverage

      - name: 🔨 Production build test
        run: npm run build

      - name: 📈 Build size check
        run: npm run size
        continue-on-error: true  # Size warnings shouldn't fail

      - name: ✅ Tüm kontroller başarılı
        run: echo "✅ All checks passed!"
